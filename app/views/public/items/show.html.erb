<table class="table">
    <thead>
    <tbody>
      <tr>
        <td>
          <%= image_tag @item.get_image(100,100) %>
        </td>
         <td>
         <%= @item.name %>
        </td>
       <td>
      <%= @item.introduction %>
    </td>
  <td>
<a>¥<%= @item.price %>(税込)</a>
</td>
</tr>
</tbody>
</table>

<%= form_with model: @cart_item, url: cart_items_path do |f| %>
<%= f.select :amount, ['1', '2', '3','4','5'], {include_blank: '個数を選択'}, class: 'sample' %>
   <%= f.submit 'カートに入れる', class: "btn btn-success" %>
  <% end %>